{
  "name": "functions",
  "description": "Cloud Functions for the Awqat prayer-times app (Claude timetable extraction proxy)",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.1.1"
  }
}
